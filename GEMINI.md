# ChangeMesh Antigravity Adapter

Antigravity must obey the repository constitution.

## Required first reads

@AGENTS.md
@AGENT_OS_RULES.md
@plans/CHANGEMESH_MASTER_EXECUTION_PLAN.md
@AGENT_MEMORY_AND_LESSONS.md
@AGENT_ARCHITECTURE_AND_PATTERNS.md
@AGENT_ENVIRONMENT_AND_API.md
@AGENT_USER_PREFERENCES.md
@docs/HANDOFF.md

## Project status

The project charter is frozen. Do not run a generic Phase-0 interview. Inspect the repository and continue from the next eligible master-plan micro-task.

## Work discipline

- Main Antigravity agent is sole writer of shared plan/governance/docs.
- Use subagents for read-only research, challenge, test verification, and consistency audit.
- Never trust a subagent summary without checking files and command output.
- Mark the task `IN_PROGRESS` before edits.
- Use a snapshot/worktree/commit boundary before a phase.
- Run Phase `P-Ω` before marking any task `DONE`.
- Never convert `NOT_RUN`, `SIMULATED`, or `BLOCKED` into `PASS`.
- Do not add routine human approvals; preserve autonomous-by-default product behavior.
