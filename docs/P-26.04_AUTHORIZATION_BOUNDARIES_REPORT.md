# P-26.04 — Authorization Boundaries and External-Write Restrictions Report

**Date:** 2026-08-22  
**Task:** `P-26.04` — Test authorization boundaries/external-write restrictions  
**Status:** `DONE` / `PASS`  
**Core Artifact:** `tests/test_p26_04_authorization_boundaries.py` (8 tests passing)  
**Total Canonical Tests Passing:** 1686 passed, 0 failures, 1 warning (12.37s)  
**Acceptance Criteria:** `No agent merges, production-deploys, or accesses unregistered data.`

---

## 1. Authorization Boundaries Verification Matrix

| Domain / Safety Boundary | Enforcing Symbol | Protection Mechanism | Outcome | Test Evidence |
|---|---|---|---|---|
| **GitHub Auto-Merge Prevention** | `GitHubAction` Enum | Enum strictly permits only `CREATE_BRANCH`, `CREATE_COMMIT`, `CREATE_DRAFT_PR`. Auto-merges and repo deletions are structurally impossible. | `PASS` | `test_github_actions_enum_forbids_merge_and_destructive_operations` |
| **Protected Branch Defense** | `BoundedGitHubAdapter.PROTECTED_BRANCHES` | Blocks direct commits and branch overwrites to `main`, `master`, `prod`, `production`, `release`. | `PASS` | `test_github_adapter_blocks_commits_to_protected_branches` |
| **Governance Path Lockdown** | `DeterministicPolicyChecker.ALLOWED_WRITE_PATHS` | Rejects modifications targeting `domain/contracts/`, `.github/`, `.env`, `/etc/`, or directory traversal attempts. | `PASS` | `test_policy_blocks_unauthorized_governance_and_system_paths` |
| **Tool Execution Whitelist** | `DeterministicPolicyChecker.ALLOWED_TOOL_IDS` | Denies tool invocations outside the registered whitelist (e.g. arbitrary shell execution). | `PASS` | `test_policy_blocks_unregistered_tools` |
| **Destructive Intent Rejection** | `validate_supported_change_intent()` | Fails closed on incoming change requests describing `DROP TABLE`, `TRUNCATE`, or `DELETE FROM`. | `PASS` | `test_destructive_sql_operations_fail_intent_validation` |
| **Reversibility Escalation** | `ReversibilityClassifier.classify_sql()` | Classifies destructive DDLs lacking down migrations as `IRREVERSIBLE_DESTRUCTIVE` and with down migrations as `HUMAN_INTERVENTION_REQUIRED`. | `PASS` | `test_destructive_sql_classified_as_irreversible_or_human_intervention` |

---

## 2. External-Write Safety Confirmation

1. **Draft PR Ceiling:** Agents cannot merge pull requests or push directly to trunk branches. All changes are surfaced as GitHub Draft PRs with explicit tamper-evident intent markers.
2. **Zero Ambient Cloud Writes:** Cloud integrations fail closed by default without the `--live-write-danger` authorization flag.
3. **Deterministic Authority Gates:** LLMs and generative models cannot bypass authorization gates, approve migrations, or manufacture credentials.
