# P-20.00 Long-Running Orchestration Donor Preflight Report

> **Task:** P-20.00 — Long-running orchestration donor preflight  
> **Phase:** P-20 — Orchestrator Saga, Recovery, and Long-Running Behavior  
> **Status:** `DONE` (Preflight & Provenance Gate Complete)  
> **Owner:** Primary ChangeMesh Agent  
> **Date:** 2026-08-19  

---

## 1. Canonical Entry State

| Field | Entry Value | Verification Proof |
|---|---|---|
| **Canonical Repository** | `zyganali-glitch/ChangeMesh` | Local worktree verified |
| **Canonical Branch** | `main` | `git branch --show-current` -> `main` |
| **Verified Remote Entry SHA** | `4dd868657888ea7b11986ef5779a37635d2019fa` | `git rev-parse origin/main` matched exactly |
| **Working Tree State** | Clean (zero uncommitted edits before preflight) | `git status` -> `nothing to commit, working tree clean` |
| **Phase & Task State** | Phase P-20 `IN_PROGRESS`, Tasks P-20.00 & P-20.01 `IN_PROGRESS` | `plans/CHANGEMESH_MASTER_EXECUTION_PLAN.md` |
| **Canonical Unit Baseline** | `1321 passed, 1 warning` | `uv run python scripts/cmd.py unit` (exit code `0`) |
| **Full Suite Baseline** | `1321 passed, 1 warning, 3 errors` | `uv run python -m pytest tests/` (exit code `1`, historical GCP fixture debt in `tests/test_gcp_access.py`) |
| **Donor Manifest Lint Baseline** | `20 components valid` | `uv run python tools/governance/donor_manifest_lint.py` (exit code `0`) |

---

## 2. Donor Provenance Matrix

All donor repositories are strictly **READ-ONLY**. Source inspection was performed exclusively against immutable pinned commits and allowlisted source paths already approved in [`docs/DONOR_REUSE_MANIFEST.md`](DONOR_REUSE_MANIFEST.md).

| Donor ID | Repository | Pinned Immutable SHA | Inspected Allowlisted Source Paths | Component ID | License State | Reuse Method | Source Behavior Actually Observed |
|---|---|---|---|---|---|---|---|
| **`D-UIPATH`** | `zyganali-glitch/universal-agent-os-uipath` | `dc2267939c2aef0aba2737da65f53352c5cf8fb2` | `backend/sync_markdown_to_uipath.py`<br>`tests/test_phase0_interview.py` | `UIPATH-STATE-001` | `VERIFIED_COMPATIBLE` | `IDEA_ONLY` | `backend/sync_markdown_to_uipath.py` reads/parses governance markdown and sends bounded governance/memory payloads through UiPath connector / Data Service (does NOT prove a generic durable saga lifecycle). `tests/test_phase0_interview.py` proves persisted Phase-0/interview state, sequential bounded interview/question progression inside Phase-0, gate-controlled refusal while gate status is `AWAITING_HUMAN`, and persisted `PHASE0_COMPLETE` state. Generic ChangeMesh states such as `RECEIVED` or the 8-stage lifecycle are NOT present in donor source and are entirely ChangeMesh-native transformations. |
| **`D-QWEN`** | `gitlab.com/zyganali/universal-agent-os-qwen` | `a43b3411856f41a4be9424d11c01a5e637cdc410` | `backend/vector_store.py`<br>`tests/test_vector_store.py` | `QW-BUS-001` | `VERIFIED_COMPATIBLE` | `CLEAN_ROOM_REIMPLEMENTED` | Vector memory retrieval and multi-agent context handoff with tenant/scope isolation. Epistemic trust classification, immutable MemoryRecord references, and deterministic policy pre-checks are ChangeMesh-native transformations. |
| **`D-CCT`** | `zyganali-glitch/codex-control-tower` | `65ee1b72faf9a7202d9166eed43fb671804815a8` | `cli/commands/flight-recorder.js`<br>`tests/test_codex_review.js` | `CCT-FLIGHT-001` | `VERIFIED_COMPATIBLE` | `CLEAN_ROOM_REIMPLEMENTED` | Append/read command wrapper for local JSON-based recording of events and timeline inspection (`flight-recorder.js`). Kahn topological sort causal DAG ordering over causation_id links, EventEnvelope schema, and payload secret sanitization are ChangeMesh-native transformations. |

---

## 3. Exact Donor Source → ChangeMesh Target Mapping

| Donor Source Pattern | ChangeMesh Architectural Target | Status / Method | What is Retained (Source-Observed) | What is Transformed (ChangeMesh-Native) | What is Excluded (Forbidden Carry-Over) | Owning Phase & Task |
|---|---|---|---|---|---|---|
| **D-UIPATH Process Progression** (`backend/sync_markdown_to_uipath.py`, `tests/test_phase0_interview.py`) | `src/orchestrator/orchestrator_saga.py` | `IDEA_ONLY` | Sequential bounded interview/question progression inside Phase-0, persisted Phase-0/interview state, gate refusal while AWAITING_HUMAN, and persisted PHASE0_COMPLETE (tested in `tests/test_phase0_interview.py`; `backend/sync_markdown_to_uipath.py` parses markdown governance). | Cleanroom Python 3.13 Google ADK + Cloud Pub/Sub + SagaStateRepository (Firestore); 8-stage canonical lifecycle (`DISCOVERING`, `QUALIFYING`, `REHEARSING`, `GROUNDED`, `AWAITING_AUTHORITY`/`BLOCKED`, `EXECUTING`, `VERIFYING`, `CERTIFYING`, `COMPLETE`); optimistic concurrency versioning; pre-persistence secret boundary; intent binding. | Maestro, Action Center, Data Service, UiPath API tokens, and Phase-0 interview semantics. | **P-20.01** |
| **D-QWEN Memory Bus & Context Handoff** (`backend/vector_store.py`) | `src/memory/` & `src/orchestrator/orchestrator_saga.py` | `CLEAN_ROOM_REIMPLEMENTED` | Scoped multi-agent memory retrieval and context exchange. | Epistemic memory trust classification (`ACCEPTED_TRUSTED` vs `QUARANTINED`); immutable `MemoryRecord` references; tenant isolation. | Qwen runtime dependency, unrestricted shared mutable memory, prompt-based memory injection. | **P-11** / **P-20.01** |
| **D-CCT Causal Flight Recorder** (`cli/commands/flight-recorder.js`) | `src/evidence/pubsub_timeline.py` & `src/orchestrator/orchestrator_saga.py` | `CLEAN_ROOM_REIMPLEMENTED` | Local chronological event recording of session progress. | Kahn DAG causal topological sorting over `causation_id` links; `EventEnvelope` schema; ingest secret sanitization; deterministic SHA-256 timeline digest. | Codex/OpenAI/GPT-specific event types, local-only unlinked chronology, UI styling carry-over. | **P-09.05** / **P-20.01** |

---

## 4. Recovery and Resume Invariant Map

```
                     ┌─────────────────────────────┐
                     │    ChangeRequest Received   │
                     │    (ChangeState.RECEIVED)   │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    1. Discover Stage        │
                     │   (ChangeState.DISCOVERING) │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    2. Qualify Stage         │
                     │   (ChangeState.QUALIFYING)  │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    3. Rehearse Stage        │
                     │   (ChangeState.REHEARSING)  │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    4. Ground Stage          │
                     │   (ChangeState.GROUNDED)    │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    5. Authorize Gate        │
                     │    (PolicyGuardianGate)     │
                     └──────┬───────────────┬──────┘
             BLOCKED        │               │  HUMAN_AUTHORITY_REQUIRED
        ┌───────────────────┘               └───────────────────┐
        ▼                                                       ▼
┌──────────────────┐                                ┌───────────────────────────┐
│ ChangeState.     │                                │ ChangeState.              │
│ BLOCKED          │                                │ AWAITING_AUTHORITY        │
│ (Terminal Halt;  │                                │ (Halts cleanly; persists  │
│ 0 execution;     │                                │ Approval Compression card;│
│ 0 mutations)     │                                │ 0 execution; 0 mutations) │
└──────────────────┘                                └───────────────────────────┘
        │                                                       │
        │ AUTHORIZED (AUTO_EXECUTE / REHEARSE_THEN_EXECUTE)     │
        └───────────────────────────┬───────────────────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    6. Execute Stage         │
                     │   (ChangeState.EXECUTING)   │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    7. Verify Stage          │
                     │   (ChangeState.VERIFYING)   │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    8. Certify Stage         │
                     │   (ChangeState.CERTIFYING)  │
                     └──────────────┬──────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │    ChangeState.COMPLETE     │
                     │  (Durable Checkpoint Saved) │
                     └─────────────────────────────┘
```

### Core Invariants:
1. **Persistence-Before-Event Consistency:** Authoritative state is committed to `SagaStateRepository` with optimistic concurrency version check *before* the corresponding `EventEnvelope` is published to the event bus or recorded in `CausalEventTimeline`. If persistence fails, zero events are emitted and state remains pristine.
2. **Authority Sovereignty:** `PolicyGuardianGate` determines authorization. If `AutonomyClass.BLOCKED`, lifecycle transitions to `ChangeState.BLOCKED` with zero approval cards. If `AutonomyClass.HUMAN_AUTHORITY_REQUIRED`, lifecycle transitions to `ChangeState.AWAITING_AUTHORITY` with a single pending `ApprovalRecord` derived from the authoritative `ApprovalCompressionCard`.
3. **No Downstream Mutations on Non-Authorized States:** Zero execution tasks, zero migration synthesis writes, and zero Release Steward actions occur after `BLOCKED` or `AWAITING_AUTHORITY`.
4. **Execution Evidence Mode Honesty:** Local operations (blast radius analysis, capability lookup, epistemic evaluation, artifact generation, semantic reconciliation) are labeled `FIXTURE` or `SIMULATION` and never `LIVE_WRITE`. ShadowLab always produces `SIMULATION` evidence. `LIVE_WRITE` requires actual credential-backed provider execution with deterministic receipts.
5. **Secret Minimization:** Credentials and secrets are adapter-only. Payloads and intent descriptions are sanitized and secret-scanned before wire message publishing and repository persistence.
6. **Strict Lifecycle Contract Safety:** `ChangeState` enum types are verified with strict `isinstance` checks; arbitrary foreign objects are rejected.

---

## 5. No-Duplicate-Write Test Plan

| Test ID | Scenario | Verification Method | Expected Outcome |
|---|---|---|---|
| `TEST-SAGA-IDEM-01` | Same `(tenant_id, change_id)` step dispatched twice | `SagaStateRepository` optimistic concurrency check | Second writer rejected with `OptimisticConcurrencyError`; exactly one `ChangeRecord` state update committed. |
| `TEST-SAGA-IDEM-02` | `transition_and_persist` failure | Persistence fails on version mismatch | No event published to `LocalEventBus`; no event added to `CausalEventTimeline`; zero duplicate events emitted. |
| `TEST-SAGA-IDEM-03` | Stopped at `AWAITING_AUTHORITY` | Inspect persisted task and approval records | Zero execution tasks executed; exactly one pending `ApprovalRecord` persisted; zero Release Steward writebacks. |
| `TEST-SAGA-IDEM-04` | Rehearsal failure in `ShadowLabRunner` | Scenario with injected fault executed | Rehearsal returns `passed=False`; Policy Guardian detects failed rehearsal and issues `AutonomyClass.BLOCKED`; zero downstream execution. |
| `TEST-SAGA-IDEM-05` | Adversarial credential injection in request description | Change request with `ghp_...` token submitted | Raw token minimized/sanitized; zero credential occurrences in wire messages, timeline, or repository documents. |
| `TEST-SAGA-IDEM-06` | Mode escalation prevention | Caller requests `evidence_mode=LIVE_WRITE` on local saga | Local tasks remain honestly labeled `FIXTURE`/`SIMULATION`; no fake `LIVE_WRITE` PASS evidence generated. |

---

## 6. Forbidden Carry-Over

1. **No UiPath runtime or SDK:** No UiPath Orchestrator, Data Service, Robot, or Action Center dependencies.
2. **No Qwen runtime or unverified memory:** No vector store bypass of epistemic trust validation; memory is never truth by default.
3. **No Codex/GPT runtime or prompt-overrides-fact semantics:** Semantic model judgment cannot rewrite deterministic execution facts or state transitions.
4. **No Routine Human Approvals:** Preserves autonomous-by-default behavior for reversible/rehearsed changes; human authority is reserved strictly for policy-defined high-risk boundaries.
5. **No Caller Fact Overrides:** Untrusted callers cannot inject `force_reversibility_class` to bypass policy rules.

---

## 7. Intentional Differences from Donor Behavior

1. **Google Cloud Native:** Architecture standardizes on Google ADK agents, Pub/Sub event bus topology, and Firestore operational state repository.
2. **Four-Lane Authority Model:** Complete separation of Organizational Policy, Deterministic Execution Facts, Policy Guardian Enforcement, and Bounded Human Authority Slots.
3. **Approval Compression:** Replaces generic human review forms with structured, machine-verified `ApprovalCompressionCard` packets linking exact locked evidence digests.
4. **Causal Timeline over Wall-Clock:** Causal Kahn DAG traversal enforces dependency ordering even in the presence of distributed clock skew.

---

## 8. Read-Only Donor Reuse Auditor Findings & P-DΩ Evaluation

- **Evaluator:** Read-Only Donor Reuse Auditor (`donor-reuse-auditor` subagent)
- **Audit Date:** 2026-08-19
- **Overall Auditor Verdict:** `PASS` (0 Blocking Findings, 0 Unresolved Ambiguities)

### P-DΩ.01–P-DΩ.08 Evaluation Matrix

| Criterion | Evaluation Summary | Verdict |
|---|---|---|
| **`P-DΩ.01` — Immutable source parity** | Verified all 3 donor pins (`dc2267939c2aef0aba2737da65f53352c5cf8fb2`, `a43b3411856f41a4be9424d11c01a5e637cdc410`, `65ee1b72faf9a7202d9166eed43fb671804815a8`) are immutable 40-character SHAs against allowlisted files. Zero moving branch references. | **PASS** |
| **`P-DΩ.02` — Manifest completeness parity** | All 16 mandatory YAML fields present for all 20 manifest components; `python tools/governance/donor_manifest_lint.py` passes with exit code `0`. | **PASS** |
| **`P-DΩ.03` — Behavioral traceability** | Full 8-stage saga lifecycle mapped and tested; clear separation of observed donor source behavior from ChangeMesh-native transformations. | **PASS** |
| **`P-DΩ.04` — License & notice parity** | All donor components are owner-authored with `VERIFIED_COMPATIBLE` license state. | **PASS** |
| **`P-DΩ.05` — Forbidden carry-over audit** | Automated keyword scans across `src/orchestrator/` confirmed 0 occurrences of UiPath SDK/runtime, Qwen SDK/unrestricted memory, OpenAI/Codex event types, or prompt-overrides-facts logic. | **PASS** |
| **`P-DΩ.06` — Canonical anti-zombie audit** | Exactly one canonical operational state store (`SagaStateRepository`) and one saga orchestrator (`ChangeSagaOrchestrator`). ADK agent `ChangeOrchestrator` delegates without owning mutable state. | **PASS** |
| **`P-DΩ.07` — Competition commit disclosure** | All introduction commits and component statuses derived from repository git history and recorded in manifest and provenance ledger. | **PASS** |
| **`P-DΩ.08` — Test and security parity** | 38 dedicated saga tests pass (`tests/test_p20_orchestrator_saga.py`); canonical unit test suite passes 1350 tests with 1 warning (`uv run python scripts/cmd.py unit`, exit code `0`). | **PASS** |
