# P-08.00 Gemini Boundary Donor Preflight Report

> **Task:** P-08.00 — Gemini boundary donor preflight
> **Phase:** P-08 — Gemini Integration and Structured Reasoning Boundary
> **Date:** 2026-08-16
> **Entry SHA:** `a00c6bce860d5a6618c7d89d63cde16704cfe4e5`
> **Previous Verified Baseline:** `1a03c8741ef1981591e93cd7870bc1266a89ef02`
> **Canonical Branch:** `main`
> **Canonical Remote:** `https://github.com/zyganali-glitch/ChangeMesh.git`

---

## 1. Entry Baseline

| Field | Value |
|---|---|
| ChangeMesh Entry SHA | `a00c6bce860d5a6618c7d89d63cde16704cfe4e5` |
| Donor D-CCT | `zyganali-glitch/codex-control-tower` |
| D-CCT immutable commit | `65ee1b72faf9a7202d9166eed43fb671804815a8` |
| Donor D-ZEROKIT | `zyganali-glitch/zerokit-ai-control-plane` |
| D-ZEROKIT immutable commit | `d663db8c706cb914e1af5caf651df08edb5c50c0` |
| P-02D status | `DONE` |
| P-07 status | `DONE` |
| P-08.01 status | `PENDING` (must remain PENDING throughout P-08.00) |

**Exact source paths inspected:**

| Component | Donor | Source Paths |
|---|---|---|
| CCT-EVID-001 | D-CCT | `cli/commands/evidence.js`, `tests/test_evidence_pack.js` |
| CCT-SEM-001 | D-CCT | `cli/commands/codex-review.js`, `tests/test_codex_review.js` |
| ZK-PRIV-001 | D-ZEROKIT | `ai-buildweek/lib/privacy-guard.mjs`, `tests/unit/privacy-guard.test.mjs` |
| ZK-VALID-001 | D-ZEROKIT | `frontend/js/config-validator.js`, `tests/unit/config-validator.test.mjs` |

No unregistered donor paths were inspected.

---

## 2. Source Behavior Evidence

### 2.1 CCT-EVID-001 — Evidence Pack / Simulation and NOT_RUN Boundary

**Donor:** D-CCT
**Immutable SHA:** `65ee1b72faf9a7202d9166eed43fb671804815a8`
**Source paths:** `cli/commands/evidence.js`, `tests/test_evidence_pack.js`
**Reuse method:** `CLEAN_ROOM_REIMPLEMENTED`
**License/notice:** `VERIFIED_COMPATIBLE` (owner-authored, no third-party license headers)

#### Behavior observed in source

1. **Evidence pack generation (`createEvidencePack(target, outDir)`):** Builds an evidence pack directory containing mandatory report artifacts (`EVIDENCE_REPORT.md`, `TRACEABILITY_MATRIX.md`, `NOT_RUN_GATES.md`, `TECH_DEBT_DELTA.md`, `CODEX_MISSION_PROMPT.md`, `CONTROL_TOWER_REPORT.json`, `CONTEXT_GRAPH.json`, `REVIEW_GATE_STATUS.md`, `MEMORY_LENS_REPORT.md`, `MISTAKE_SHIELD_REPORT.md`, `FLIGHT_RECORDER_SUMMARY.md`, `DEVPOST_SUMMARY.md`).
2. **Explicit execution boundary disclosure:** `cli/commands/evidence.js` explicitly prints: `Boundary: ${result.report.simulatedData ? 'SIMULATED' : 'REAL_LOCAL_SCAN'}; target tests remain NOT_RUN.`
3. **Simulation disclosure:** `report.simulatedData === true` is recorded in `CONTROL_TOWER_REPORT.json` and matched in `EVIDENCE_REPORT.md`.
4. **NOT_RUN gate disclosure:** Explicitly recorded and matched in `NOT_RUN_GATES.md`.
5. **Deterministic rerun idempotency:** Rerunning evidence pack generation on the same target does not alter `after.score` (`after.score === before.score`), does not alter `after.riskFlags.length` (`after.riskFlags.length === before.riskFlags.length`), and does not add new nodes to its own context graph (`after.contextGraph.nodes.length === before.contextGraph.nodes.length`).

#### Tests that actually prove behavior

- `tests/test_evidence_pack.js` asserts all 12 required evidence-pack files exist in the output directory.
- Asserts `report.simulatedData === true`.
- Asserts `NOT_RUN_GATES.md` contains `/NOT_RUN/`.
- Asserts `EVIDENCE_REPORT.md` contains `/SIMULATED/`.
- Asserts rerun produces identical score (`after.score === before.score`).
- Asserts rerun produces identical risk flag count (`after.riskFlags.length === before.riskFlags.length`).
- Asserts rerun does not enter its own context graph (`after.contextGraph.nodes.length === before.contextGraph.nodes.length`).

#### What is NOT proven by these allowlisted files

- No frozen 7-state EvidenceState enum in these two files.
- No immutable deterministic-facts set list in these two files.
- No `validateModelCannotOverwriteFact()` function in these two files.
- No model-promotion rejection logic in these two files (grounded in `codex-review.js` and ChangeMesh's own contracts).
- No content-addressed pack sealing in these two files.
- No model/fact reconciliation in these two files.
- *Note:* ChangeMesh's broader requirement that model judgment cannot overwrite deterministic facts is grounded in CCT-SEM-001 and ChangeMesh's own frozen authority and evidence contracts (`domain/contracts/evidence.py`, `docs/AUTHORITY_MAP.md`, `docs/EVIDENCE_BOUNDARY.md`).

#### Exact ChangeMesh target mapping

- `src/evidence/evidence_record.py` (exact manifest target)
- *Existing related ChangeMesh contract/context:* `domain/contracts/evidence.py` (EvidenceState, EvidenceRecord already implemented in P-05.03)

#### Duplicate/conflicting implementation risk

ChangeMesh `domain/contracts/evidence.py` implements `EvidenceState` enum with 7 states. The CCT-EVID-001 observable invariants (boundary disclosure, simulation flag preservation, NOT_RUN visibility, rerun idempotency) inform the runtime evidence record that P-08 and later phases will build. No conflict.

#### Forbidden carry-over

- `project: 'codex-control-tower'` (Codex project name)
- Codex event assumptions
- GPT-specific fields
- `InvoiceFlow` fixture identity

---

### 2.2 CCT-SEM-001 — Blind Semantic Review / Model-Fact Separation

**Donor:** D-CCT
**Immutable SHA:** `65ee1b72faf9a7202d9166eed43fb671804815a8`
**Source paths:** `cli/commands/codex-review.js`, `tests/test_codex_review.js`
**Reuse method:** `CLEAN_ROOM_REIMPLEMENTED`
**License/notice:** `VERIFIED_COMPATIBLE` (owner-authored, no third-party license headers)

#### Behavior observed in source

1. **Neutral deterministic claims:** `CLAIM_DEFINITIONS` template list defines claim IDs (`TEST_EXECUTION`, `CI_EXECUTION`, `REVIEW_GATE`, `EVIDENCE_BOUNDARY`, `EXTERNAL_GATES`, `MISSION_CHANGE_TEST_ALIGNMENT`), evidence status keys, and fallback paths. `buildDeterministicClaims(report)` generates claims with `id`, `claim` (neutral claim text without disclosing pass/fail expectation), `claimKind` (`LOCKED_EVIDENCE_STATE_CLAIM` or `SEMANTIC_CHALLENGE_WITH_STRUCTURAL_PRECHECK`), `deterministicStatus`, `deterministicBasis`, and `evidencePaths`.
2. **State vocabulary:** `DETERMINISTIC_STATES = ['PASS', 'WARN', 'FAIL', 'NOT_RUN', 'SIMULATED']`; `MODEL_ASSESSMENTS = ['SUPPORTS', 'CONTRADICTS', 'INSUFFICIENT']`.
3. **Prompt isolation from local expected statuses:** `buildReviewPrompt(report, claims, evidenceBundle, options)` embeds neutral claims and bounded raw evidence into the prompt while strictly withholding the reconciler's locked claim statuses and expected comparison classes ("Real GPT-5.6 blind semantic audit: the model receives neutral claims and bounded raw evidence in an isolated empty workspace, while the reconciler's locked claim statuses and expected comparison classes are withheld. Local states remain locked and independently reproducible.").
4. **Bounded evidence bundle & integrity:** `buildEvidenceBundle` reads allowed relative evidence paths, rejects path traversal and unsafe characters via `safeEvidencePath` / `reportedEvidencePath`, and `collectEvidenceIntegrity` computes SHA-256 digests and Git worktree snapshot (`PRE_RUN` / `COLLECTION_TIME`).
5. **Event stream policy:** `validateCodexEventStream` enforces `policy: 'NO_TOOL_EVENTS'`, validates events against `SAFE_CODEX_EVENT_TYPES` and `SAFE_CODEX_ITEM_TYPES` (`agent_message`, `reasoning`), rejects tool executions, requires exactly 1 agent message and 1 turn.
6. **Strict structured assessment validation:** `RECONCILIATION_SCHEMA` and `validateModelAssessment` enforce schema adherence, exact 1-to-1 claim audit matching, reject duplicate IDs, reject unauthorized field injections (such as `deterministicStatus` or top-level `verdict`), reject model claims of executing tests, enforce citations for decisive assessments (`SUPPORTS`, `CONTRADICTS`), require `counterEvidence` for `CONTRADICTS`, and require `missingEvidence` for `INSUFFICIENT`.
7. **Deterministic reconciliation & fact sovereignty:** `reconcileAssessment` reconciles the model assessment with locked deterministic claim statuses. The model verdict is strictly advisory (`modelVerdict`). The deterministic verdict (`deterministicVerdict`) remains sovereign. An optimistic model response (`SUPPORTS`) cannot promote a locked `NOT_RUN`, `FAIL`, or `SIMULATED` state. Any model/fact disagreement triggers `reviewState = 'HUMAN_REVIEW_REQUIRED'` and `humanReviewRequired = true`.

#### Tests that actually prove behavior

- `tests/test_codex_review.js` tests safe assessment reconciliation: matching deterministic and model statuses yields agreement.
- Contradiction on passing claim creates `alignmentConflict.agreement = 'DISAGREEMENT'`, `reviewState = 'HUMAN_REVIEW_REQUIRED'`, `deterministicVerdict = 'FAIL'`.
- Optimistic model response on locked `NOT_RUN` claim retains `deterministicStatus = 'NOT_RUN'`, `deterministicVerdict = 'WARN'`, `reviewState = 'HUMAN_REVIEW_REQUIRED'`.
- Schema violations: missing claim throws `/exactly one audit/`, duplicate ID throws `/Unknown or duplicate/`, injected `deterministicStatus` throws `/unauthorized field/`, injected top-level `verdict` throws `/unauthorized top-level field/`, malformed citation throws `/Invalid cited evidence paths/`, legacy question assessment throws `/Invalid model assessment/`, missing counterEvidence throws `/Invalid counter evidence/`, decisive assessment without allowed citation throws `/requires at least one allowed citation/`, contradiction without counterEvidence throws `/requires counter evidence/`, insufficient without missingEvidence throws `/requires missing evidence/`, claim of test execution throws `/claimed it executed/`.
- Event stream validation: safe event stream passes with `NO_TOOL_EVENTS`; command execution event throws `/unapproved tool or item type/`; non-JSON throws `/non-JSON audit event/`; turn.failed throws `/turn.failed/`; plan_update throws `/unapproved tool or item type/`.
- Recorded report context: stale report (>24h) records `stale = true`, `status = 'WARN'`.

#### What is NOT proven by the donor

- No frozen 4-authority-class enum `MODEL_SEMANTIC_JUDGMENT` in this file (ChangeMesh defines `GEMINI_SEMANTIC_JUDGMENT` in `docs/AUTHORITY_MAP.md`).
- No `prepareBlindBundle` function with 9 hardcoded exclusion keys throwing `BLIND REVIEW VIOLATION` (prompt isolation is achieved via `buildDeterministicClaims` / `buildReviewPrompt` withholding expected statuses, and `validateModelAssessment` forbidding injected fields).
- No Gemini / ADK integration (donor uses `@openai/codex` CLI and `gpt-5.6-sol`).

#### Exact ChangeMesh target mapping

- `src/agents/evidence_auditor.py` (exact manifest target)

#### Duplicate/conflicting implementation risk

Authority classes are defined in ChangeMesh's `docs/AUTHORITY_MAP.md` and `docs/EVIDENCE_BOUNDARY.md` with the name `GEMINI_SEMANTIC_JUDGMENT` (not `MODEL_SEMANTIC_JUDGMENT`). This is an intentional transformation — no conflict.

#### Forbidden carry-over

- `@openai/codex/bin/codex.js` runtime
- ChatGPT login check (`login status`)
- `gpt-5.6-sol` model identifier
- `MODEL_SEMANTIC_JUDGMENT` (must be `GEMINI_SEMANTIC_JUDGMENT` in ChangeMesh)
- `SAFE_CODEX_EVENT_TYPES`, `SAFE_CODEX_ITEM_TYPES`
- `codex-review` CLI command name
- `InvoiceFlow` references

---

### 2.3 ZK-PRIV-001 — Privacy Guard / Input Minimization

**Donor:** D-ZEROKIT
**Immutable SHA:** `d663db8c706cb914e1af5caf651df08edb5c50c0`
**Source paths:** `ai-buildweek/lib/privacy-guard.mjs`, `tests/unit/privacy-guard.test.mjs`
**Reuse method:** `CLEAN_ROOM_REIMPLEMENTED`
**License/notice:** `VERIFIED_COMPATIBLE` (owner-authored, no third-party license headers)

#### Behavior observed in source

1. **Pre-send privacy scanning:** Scans text for private keys (`-----BEGIN...PRIVATE KEY-----`), OpenAI-looking API keys (`sk-proj-...`), GitHub tokens (`ghp_/gho_/ghu_/ghs_/ghr_`), AWS keys (`AKIA.../ASIA...`), JWTs (`eyJ...`), Authorization Bearer headers, database connection strings with passwords, and session cookies.
2. **Two-tier severity model:** Blockers (`severity: 'block'`) cause fail-closed rejection (`safeToSend: false`, blockers list populated); review patterns (`severity: 'review'`) flag UUIDs, public IPs, and production data markers (`production dump`, `patient record`, `student record`) as review-severity findings in `reviewItems`.
3. **Reserved synthetic email domain allowlisting:** Emails ending in `@example.com`, `@example.net`, `@example.org`, `@example.test` pass as synthetic fixture data.
4. **`auditPrivacyText(text)`:** Returns `{safeToSend, findings, blockers, reviewItems}`.
5. **`assertPrivacySafe(text)`:** Throws on blockers (fail-closed).

#### Tests that actually prove behavior

`tests/unit/privacy-guard.test.mjs` explicitly tests and proves:
- Synthetic text with reserved email domains passes (`safeToSend: true`, `blockers: []`).
- Real-looking email addresses are blocked.
- Authorization Bearer tokens are blocked.
- OpenAI-looking API keys are blocked and cause `assertPrivacySafe` to throw.
- UUIDs trigger review findings (`reviewItems` populated).
- Production data markers trigger review findings (`reviewItems` populated).

#### What is NOT proven by the donor

- No field-level allowlist/denylist for structured prompt objects (operates on raw text).
- No Gemini-specific SDK integration.
- No structured prompt construction.

#### Exact ChangeMesh target mapping

- `src/policy/policy_guardian.py` (exact manifest target)
- *Existing related ChangeMesh contract/context:* `domain/contracts/conventions.py` (`redact_mapping` with `REDACTION_SENTINEL = "[REDACTED]"`)

#### Duplicate/conflicting implementation risk

ChangeMesh `domain/contracts/conventions.py` implements `redact_mapping()`. The privacy guard in ZK-PRIV-001 is an input-side scanner (prevent model input), while `redact_mapping` is an output-side structural redactor. They serve complementary roles — no conflict.

#### Forbidden carry-over

- `openai_api_key` regex pattern name
- School-SaaS fixture scenarios (`school-saas`)
- ZeroKit product semantics

---

### 2.4 ZK-VALID-001 — Config Validator / Strict Output Validation

**Donor:** D-ZEROKIT
**Immutable SHA:** `d663db8c706cb914e1af5caf651df08edb5c50c0`
**Source paths:** `frontend/js/config-validator.js`, `tests/unit/config-validator.test.mjs`
**Reuse method:** `CLEAN_ROOM_REIMPLEMENTED`
**License/notice:** `VERIFIED_COMPATIBLE` (owner-authored, no third-party license headers)

#### Behavior observed in source

1. **Multi-section schema validation:** Requires root plain object and required object sections (`panel_registry`, `rbac_registry`, `field_registry`, `endpoint_map`).
2. **Type checking:** Boolean flags (`enabled`, `visible`) must be boolean; panel `order` must be a finite number (`Number.isFinite(panel.order)`); not required to be an integer by the donor.
3. **Endpoints:** Non-root-relative or absolute adapter-like endpoint produces a WARNING in the donor (`warnings.push(...)`), rather than rejection.
4. **`validateZeroKitConfig(config)`:** Returns `{valid, errors, warnings, stats}`.
5. **Fail closed:** Missing required sections or malformed fields produce errors that set `valid: false`.

#### Tests that actually prove behavior

`tests/unit/config-validator.test.mjs` explicitly tests and proves:
- Valid scenario configs (`school-saas`, `healthcare-saas`, `agency-saas`) produce `valid: true`, 0 warnings, valid stats (`stats.panels >= 8`).
- Negative test: malformed panel/role/field/endpoint config produces `valid: false` with expected error messages.

#### What is NOT proven by the donor / Intentional ChangeMesh Delta

- Donor accepts finite numbers for order; ChangeMesh may enforce strict Pydantic typed fields.
- Donor only warns on non-root-relative endpoints; ChangeMesh intentionally requires strict path/endpoint rejection (OUT-T05, OUT-T06) as an intentional ChangeMesh delta / future test.
- Donor does not validate Gemini structured outputs (browser-context JS validator).

#### Exact ChangeMesh target mapping

- `src/core/gemini_structured_output.py` (exact manifest target)
- *Existing related ChangeMesh contract/context:* ChangeMesh Pydantic domain contracts (`domain/contracts/*`)

#### Duplicate/conflicting implementation risk

ChangeMesh Pydantic contracts already enforce strict schemas with `extra="forbid"`. ZK-VALID-001's JavaScript validator pattern will inform the Gemini structured output validator design. No conflict.

#### Forbidden carry-over

- ZeroKit configuration schema (`panel_registry`, `rbac_registry`, `field_registry`, `endpoint_map`)
- `validateZeroKitConfig` function name
- `school-saas`, `healthcare-saas`, `agency-saas` fixture scenarios
- Frontend globals

---

## 3. Prompt / Input Boundary Memo

> **Scope:** Defines requirements for later Gemini calls (P-08.01, P-08.03). Does NOT implement them.

### 3.1 Field-Level Input Minimization

**INP-01:** Only task-minimal allowlisted fields may reach the model. A Gemini prompt must contain only the fields explicitly required for the semantic task. No evidence pack, no complete change record, no full blast-radius graph is sent wholesale.

**INP-02:** Deterministic facts are externally locked before the model sees them. The model receives facts as read-only context but cannot rewrite their execution state, evidence mode, hash, or exit code.

**INP-03:** Model receives facts but cannot rewrite their execution state. Following CCT-SEM-001 reconciliation invariant (where donor locks `NOT_RUN`, `FAIL`, `SIMULATED`, `WARN`, `PASS` against model promotion) and ChangeMesh's own canonical extension (where `BLOCKED` and `QUARANTINED` are also sovereign deterministic states in `EvidenceState`): the model's judgment output is validated against locked facts. Any attempt to promote `NOT_RUN`, `BLOCKED`, `SIMULATED`, `FAIL`, or `QUARANTINED` to `PASS` is rejected deterministically.

### 3.2 Credential Boundary

**INP-04:** Credentials NEVER reach prompts. Following `docs/THREAT_MODEL.md` TB-08 and `AGENT_ENVIRONMENT_AND_API.md`: API keys, tokens, ADC material, cookies, service-account JSON, GitHub tokens, and all credential material must be stripped before any text reaches the Gemini API. The ZK-PRIV-001 blocking pattern approach (regex-based pre-scan) informs this, but ChangeMesh must implement it as a deterministic gate, not just a pattern scanner.

**INP-05:** Secret/PII handling before model boundary. Following ZK-PRIV-001: text is scanned for private keys, API keys, JWTs, bearer tokens, connection strings, and production data markers BEFORE entering the prompt. Blockers cause fail-closed rejection; review-severity findings (`reviewItems`) are flagged for review (with specific logging/handling policy owned by future P-08.03 implementation).

### 3.3 Untrusted Content Boundary

**INP-06:** External/untrusted text remains data and cannot become system authority. Following `docs/THREAT_MODEL.md` §9: GitHub repository content, metadata graph results, tool output, and user-supplied text are strictly data. They cannot modify system prompts, override policy, change authority classes, or become organizational policy.

**INP-07:** Synthetic vs real input is explicit. Following `docs/EVIDENCE_BOUNDARY.md`: every input to the model must carry explicit execution mode provenance (`FIXTURE`, `SIMULATION`, `RECORDED_CLOUD`, `LIVE_WRITE`). Synthetic demo fixtures must not be passed as production evidence.

### 3.4 Provenance and Authority

**INP-08:** Exact agent/change provenance remains available where required. Following P-07.05: agent `agent_id` and `agent_revision` are available for inclusion in prompts where provenance is relevant (e.g., evidence auditor knowing which agent produced evidence).

**INP-09:** Authority lane of every model-produced conclusion is `GEMINI_SEMANTIC_JUDGMENT` only. Following `docs/AUTHORITY_MAP.md`: model output never acquires `DETERMINISTIC_CODE`, `ORGANIZATIONAL_POLICY`, or `HUMAN_AUTHORITY` status.

**INP-10:** Model uncertainty cannot create authorization or `HUMAN_AUTHORITY`. Following architecture invariant 5.6: when Gemini is uncertain, the system uses validation, retry, or fail-closed — never manufacturing a human-authority slot.

### 3.5 Anti-Leakage

**INP-11:** No hidden prompt fallback. If the primary Gemini model is unavailable, the system fails closed. No silent fallback to another model, cached response, or hardcoded default.

**INP-12:** No silent provider fallback. The system does not silently switch from Gemini to another provider. Provider identity is explicit in all evidence records.

---

## 4. Output Boundary Memo

> **Scope:** Defines requirements for later structured reasoning/output work (P-08.02). Does NOT implement them.

### 4.1 Untrusted Model Output

**OUT-01:** Model output is untrusted until deterministic schema validation. Following ZK-VALID-001 invariant: every Gemini structured output must pass a Pydantic schema validator before consumption. The schema must be defined by ChangeMesh domain contracts, not by the model.

**OUT-02:** Missing required fields fail closed. If a required schema field is absent from the model's output, the entire response is rejected. No default-filling is permitted.

**OUT-03:** Extra forbidden fields fail closed. If the model's output contains fields not in the expected schema, the response is rejected (Pydantic `extra="forbid"` pattern already established in P-05 contracts).

**OUT-04:** Wrong types fail closed. If a field has the wrong type (e.g., string where integer expected), the response is rejected.

**OUT-05:** Malformed output is not silently coerced. No auto-repair, no "closest valid value" substitution, no silent type conversion.

**OUT-06:** Unsafe endpoint/path/action values fail. Path traversal (`../`), absolute paths outside scope, unknown action types, and unregistered endpoints are rejected deterministically (ChangeMesh intentional delta over donor).

**OUT-07:** Provider-specific hidden fields are forbidden. The model's response must not contain Google-internal, SDK-internal, or debugging fields that leak into domain contracts.

### 4.2 Semantic Output Cannot Overwrite Facts

**OUT-08:** Semantic output cannot overwrite deterministic `EvidenceState`, command result, hash, execution mode, authority decision, or approval fact. Following CCT-SEM-001: deterministic reconciliation pattern.

**OUT-09:** No expected-answer leakage for blind semantic audit. Following CCT-SEM-001: when the Evidence Auditor (Gemini) assesses evidence, the expected result is never included in the prompt. The model sees only neutral claims and bounded evidence.

**OUT-10:** Citations/evidence references must remain distinguishable from model prose. The model's output must structurally separate citations (pointers to specific evidence) and counter-evidence from generated explanations.

---

## 5. Model / Fact Separation Matrix

| Example | Authority Lane | Can Model Disagree? | Can Model Transfer to Another Lane? |
|---|---|---|---|
| **Command ran or did not run** | `DETERMINISTIC_CODE` | Model may comment, but `command_ran` fact is immutable | **NO** |
| **Exit code** | `DETERMINISTIC_CODE` | Model may assess quality, but `exit_code` value is immutable | **NO** |
| **Test count / pass / fail counts** | `DETERMINISTIC_CODE` | Model cannot change `test_count`, `test_pass_count`, `test_fail_count` | **NO** |
| **Artifact hash** | `DETERMINISTIC_CODE` | `artifact_hash` is SHA-256 deterministic output | **NO** |
| **Lifecycle transition** | `DETERMINISTIC_CODE` | Saga state machine owns transitions | **NO** |
| **Evidence mode/state** | `DETERMINISTIC_CODE` | `SIMULATED` remains `SIMULATED`; `NOT_RUN` remains `NOT_RUN` | **NO** |
| **Existence of approval record** | `DETERMINISTIC_CODE` | Approval either exists in durable store or does not | **NO** |
| **Semantic sufficiency** | `GEMINI_SEMANTIC_JUDGMENT` | This IS the model's lane — advisory assessment | N/A (already semantic) |
| **Goal decomposition quality** | `GEMINI_SEMANTIC_JUDGMENT` | Model interprets change request semantics | N/A |
| **Explanation** | `GEMINI_SEMANTIC_JUDGMENT` | Model provides reasoning narrative | N/A |
| **Advisory audit interpretation** | `GEMINI_SEMANTIC_JUDGMENT` | Model assesses evidence coverage | N/A |
| **Normative autonomy requirements** | `ORGANIZATIONAL_POLICY` | Model may help interpret, but policy table is deterministic | **NO** — model cannot create or override policy |
| **Authority classification rules** | `ORGANIZATIONAL_POLICY` | Policy Guardian enforces; model cannot modify | **NO** |
| **Only policy-defined bounded decisions** | `HUMAN_AUTHORITY` | Only within explicit policy slots | **NO** — model uncertainty cannot create human-authority slots |

### Explicit Lane Transfer Prohibition

**Model disagreement cannot transfer a fact between lanes:**

| Scenario | Deterministic Fact | Model Claim | Resolution |
|---|---|---|---|
| Test failed but model says "semantically acceptable" | `FAIL` (DETERMINISTIC_CODE) | "PASS" (GEMINI_SEMANTIC_JUDGMENT) | Fact remains `FAIL`. Model assessment recorded as advisory CONFLICT. |
| Command was `NOT_RUN` but model says "effectively passed" | `NOT_RUN` (DETERMINISTIC_CODE) | "PASS" (GEMINI_SEMANTIC_JUDGMENT) | Fact remains `NOT_RUN`. Model opinion is rejected. |
| Simulation passed but model claims "real execution" | `SIMULATED` (DETERMINISTIC_CODE) | "LIVE_WRITE" (GEMINI_SEMANTIC_JUDGMENT) | Mode remains `SIMULATED`. Model cannot change execution mode. |
| Model requests approval without policy slot | No HUMAN_AUTHORITY slot defined | Model suggests escalation | Request rejected. Gemini uncertainty → fail-closed, not human escalation. |

---

## 6. Adversarial Test List

> **Scope:** 36 unique adversarial test definitions across 5 categories. Mapped to future owning tasks. **DEFINED BUT NOT YET EXECUTED** in P-08.00 (execution occurs in owning implementation tasks).

### 6.1 Privacy / Minimization Tests (8 tests)

| Test ID | Description | Expected Result |
|---|---|---|
| PRIV-01 | Secret-bearing input (private key) reaches prompt builder | **BLOCKED** — fail closed |
| PRIV-02 | PII input (real email, phone) reaches prompt builder | **BLOCKED** or **REDACTED** |
| PRIV-03 | Non-allowlisted field in structured prompt context | **REJECTED** — field stripped |
| PRIV-04 | Synthetic fixture labeled as real production data | **REJECTED** — mode mismatch |
| PRIV-05 | Credential material (bearer token, API key) in prompt text | **BLOCKED** — fail closed |
| PRIV-06 | Malicious external prompt-injection text in GitHub PR body | **DATA** — treated as untrusted data, not instruction |
| PRIV-07 | Connection string with password in evidence description | **BLOCKED** — fail closed |
| PRIV-08 | JWT token in change request description | **BLOCKED** — fail closed |

### 6.2 Strict Output Validation Tests (9 tests)

| Test ID | Description | Expected Result |
|---|---|---|
| OUT-T01 | Missing required field in model response | **REJECTED** — fail closed |
| OUT-T02 | Extra field not in schema | **REJECTED** — `extra="forbid"` |
| OUT-T03 | Wrong type (string where int expected) | **REJECTED** — type validation |
| OUT-T04 | Invalid enum value in model response | **REJECTED** — enum validation |
| OUT-T05 | Path traversal (`../../../etc/passwd`) in endpoint/path field | **REJECTED** — path validation (ChangeMesh delta) |
| OUT-T06 | Unsafe endpoint (external URL) in action field | **REJECTED** — allowlist check (ChangeMesh delta) |
| OUT-T07 | Unknown action type in model response | **REJECTED** — enum/allowlist |
| OUT-T08 | Malformed JSON from model (incomplete, syntax error) | **REJECTED** — parse failure, not coerced |
| OUT-T09 | Silent coercion attempt (wrong type auto-fixed) | **REJECTED** — no coercion permitted |

### 6.3 Blind Semantic Audit Tests (7 tests)

| Test ID | Description | Expected Result |
|---|---|---|
| AUDIT-01 | Expected-label leakage: `expected_result` field in prompt | **REJECTED** — blind review violation |
| AUDIT-02 | Expected-answer hints: `should_pass` in evidence | **REJECTED** — blind review violation |
| AUDIT-03 | Uncited decisive answer: SUFFICIENT without citations | **REJECTED** — citation required |
| AUDIT-04 | Controlled mission gap: evidence doesn't cover mission | **INSUFFICIENT** expected |
| AUDIT-05 | Model disagrees with deterministic FAIL | Fact remains `FAIL`; advisory CONFLICT recorded |
| AUDIT-06 | Model disagrees with NOT_RUN | Fact remains `NOT_RUN`; model opinion rejected |
| AUDIT-07 | Model disagrees with SIMULATED | Mode remains `SIMULATED`; cannot promote to live |

### 6.4 Authority Tests (5 tests)

| Test ID | Description | Expected Result |
|---|---|---|
| AUTH-T01 | Gemini requests human approval without policy slot | **REJECTED** — no HUMAN_AUTHORITY manufactured |
| AUTH-T02 | Gemini attempts to authorize live write | **REJECTED** — model cannot acquire ORGANIZATIONAL_POLICY |
| AUTH-T03 | Gemini claims a deterministic action ran | **REJECTED** — fact remains NOT_RUN unless deterministic proof exists |
| AUTH-T04 | Gemini attempts to change EvidenceState | **REJECTED** — DETERMINISTIC_CODE is sovereign |
| AUTH-T05 | Gemini output sets approval flag | **REJECTED** — approval requires HUMAN_AUTHORITY via policy slot |

### 6.5 Forbidden Carry-Over Tests (7 tests)

| Test ID | Description | Expected Result |
|---|---|---|
| CARRY-01 | Codex/OpenAI/GPT runtime identifiers in code | **FAIL** — search must return 0 hits |
| CARRY-02 | ZeroKit product/admin identifiers in code | **FAIL** — search must return 0 hits |
| CARRY-03 | School-SaaS/customer fixture identity in code | **FAIL** — no `school-saas` or `healthcare-saas` identity |
| CARRY-04 | InvoiceFlow fixture identity in code | **FAIL** — no InvoiceFlow references |
| CARRY-05 | Donor-specific schema fields (panel_registry, rbac_registry) | **FAIL** — no ZeroKit config schema |
| CARRY-06 | `MODEL_SEMANTIC_JUDGMENT` instead of `GEMINI_SEMANTIC_JUDGMENT` | **FAIL** — must use ChangeMesh authority name |
| CARRY-07 | `gpt-5.6-sol` or any GPT model identifier | **FAIL** — must use `gemini-3.6-flash` |

**Total Adversarial Test Definitions: 36 unique tests**
Arithmetic: 8 (Privacy) + 9 (Output) + 7 (Audit) + 5 (Authority) + 7 (Carry-Over) = 36.
*Note on Execution Status:* `DEFINED != EXECUTED`. These 36 test definitions are formally specified and mapped to future owning implementation tasks. They were not executed at runtime during P-08.00.

---

## 7. Future-Phase Ownership Map

| Test Category | Test IDs | Count | Future Owning Task |
|---|---|---:|---|
| Privacy / Minimization | PRIV-01–08 | 8 | **P-08.03** — Implement prompt/input minimization and redaction before model calls |
| Strict Output Validation | OUT-T01–09 | 9 | **P-08.02** — Implement schema-constrained prompts/parsers for goal decomposition, policy explanation, semantic audit |
| Blind Semantic Audit | AUDIT-01–07 | 7 | **P-08.04** — Separate deterministic facts from model-visible evidence and withhold expected semantic classifications from auditor |
| Authority | AUTH-T01–05 | 5 | **P-08.04** and **P-25** (Comprehensive Test and Gate Matrix) |
| Forbidden Carry-Over | CARRY-01–07 | 7 | Each owning implementation phase (P-08.01 through P-08.04) and **P-25** |
| **Total** | | **36** | |

**No later task was renamed. No P-08.01 through P-08.05 behavior was implemented in P-08.00.**

---

## 8. Existing Environment Facts (Recorded, Not Re-Decided)

Per `AGENT_ENVIRONMENT_AND_API.md`:

| Fact | Value | Owner Phase |
|---|---|---|
| Gemini model ID | `gemini-3.6-flash` | P-08 |
| Python SDK | `google-genai` | P-06 |
| Vertex AI location | `global` | P-02 |

These are recorded as environment facts. P-08.00 does not re-decide them.

---

## 9. P-08.00 Scope Verification

| Scope Check | Result |
|---|---|
| Product runtime code created? | **NO** — zero `src/` files modified |
| Domain contracts modified? | **NO** |
| Gemini prompts/parsers added? | **NO** |
| Redaction runtime added? | **NO** |
| Model client created? | **NO** |
| Retry/timeout/token/safety config added? | **NO** |
| Telemetry runtime added? | **NO** |
| Live Gemini call made? | **NO** |
| Donor repositories modified? | **NO** |
| Unregistered donor path inspected? | **NO** |
| P-08.01 started? | **NO** — remains `PENDING` |

---

## 10. Read-Only Donor-Reuse Auditor Evidence

> **Auditor Subagent:** `donor-reuse-auditor` (Conversation ID: `52a0ab57-429c-4965-bfe4-d6a643eea46c`)
> **Mode:** Read-Only Adversarial Audit
> **Audit Date:** 2026-08-16
> **Overall Verdict:** `PASS`

### 10.1 Itemized Component Audit Verdicts

#### 1. CCT-EVID-001 — Evidence Pack / Simulation & NOT_RUN Boundary
- **Component ID:** `CCT-EVID-001`
- **Donor Repository:** `zyganali-glitch/codex-control-tower` (`D-CCT`)
- **Immutable SHA:** `65ee1b72faf9a7202d9166eed43fb671804815a8`
- **Exact Source Paths Checked:** `cli/commands/evidence.js`, `tests/test_evidence_pack.js`
- **Source-Behavior Verdict:** `PASS` — Evidenced strictly by observable code and tests (`createEvidencePack()` builds 12 required report artifacts; boundary disclosure `Boundary: ${result.report.simulatedData ? 'SIMULATED' : 'REAL_LOCAL_SCAN'}; target tests remain NOT_RUN.`; `report.simulatedData === true` recorded; rerun idempotency proven for score, risk flags, and context graph node count). Unproven claims (7-state enum, model reconciliation) truthfully disclosed as absent from donor files.
- **License/Notice Verdict:** `PASS` (`VERIFIED_COMPATIBLE` — owner-authored).
- **Reuse-Method Verdict:** `PASS` (`CLEAN_ROOM_REIMPLEMENTED` — Python rewrite for ADK/tool events, Change IDs, cloud traces).
- **Exact Target Mapping Verdict:** `PASS` (`src/evidence/evidence_record.py`).
- **Duplicate/Conflict-Risk Verdict:** `PASS` (No conflict with `domain/contracts/evidence.py`; informs runtime evidence record creation).
- **Forbidden-Carry-Over Verdict:** `PASS` (Excludes Codex event assumptions, GPT-specific fields, `InvoiceFlow` fixtures, and `codex-control-tower` project labels).
- **Required-Test Verdict:** `PASS` (Artifact generation, SIMULATED/NOT_RUN boundary disclosures, rerun idempotency, forbidden carry-over; `DEFINED != EXECUTED` distinction maintained).
- **WARN Findings:** None.
- **BLOCKED Findings:** None.

#### 2. CCT-SEM-001 — Blind Semantic Review / Model-Fact Separation
- **Component ID:** `CCT-SEM-001`
- **Donor Repository:** `zyganali-glitch/codex-control-tower` (`D-CCT`)
- **Immutable SHA:** `65ee1b72faf9a7202d9166eed43fb671804815a8`
- **Exact Source Paths Checked:** `cli/commands/codex-review.js`, `tests/test_codex_review.js`
- **Source-Behavior Verdict:** `PASS` — Evidenced strictly by observable code and tests (neutral claims via `buildDeterministicClaims()`; prompt isolation in `buildReviewPrompt()` withholding expected statuses; strict schema validation in `validateModelAssessment()` forbidding unauthorized injected fields and rejecting claims of test execution; `validateCodexEventStream()` enforcing `NO_TOOL_EVENTS`; deterministic reconciliation in `reconcileAssessment()` keeping deterministic facts sovereign, preventing model promotion of `NOT_RUN`/`FAIL`/`SIMULATED`, and routing conflicts to `HUMAN_REVIEW_REQUIRED`). `MODEL_SEMANTIC_JUDGMENT` enum and Gemini integrations truthfully disclosed as absent from donor.
- **License/Notice Verdict:** `PASS` (`VERIFIED_COMPATIBLE` — owner-authored).
- **Reuse-Method Verdict:** `PASS` (`CLEAN_ROOM_REIMPLEMENTED` — Python rewrite for Gemini structured output, ADK bundles, `GEMINI_SEMANTIC_JUDGMENT`).
- **Exact Target Mapping Verdict:** `PASS` (`src/agents/evidence_auditor.py`).
- **Duplicate/Conflict-Risk Verdict:** `PASS` (Integrates cleanly with `docs/AUTHORITY_MAP.md` authority definitions).
- **Forbidden-Carry-Over Verdict:** `PASS` (Excludes `@openai/codex` runtime, ChatGPT login check, `gpt-5.6-sol` model identifiers, `MODEL_SEMANTIC_JUDGMENT` name, and `InvoiceFlow` references).
- **Required-Test Verdict:** `PASS` (Prompt isolation, schema validation & field rejection, decisive citation requirements, counter-evidence enforcement, deterministic reconciliation & locked state preservation, forbidden carry-over; `DEFINED != EXECUTED` distinction maintained).
- **WARN Findings:** None.
- **BLOCKED Findings:** None.

#### 3. ZK-PRIV-001 — Privacy Guard / Input Minimization
- **Component ID:** `ZK-PRIV-001`
- **Donor Repository:** `zyganali-glitch/zerokit-ai-control-plane` (`D-ZEROKIT`)
- **Immutable SHA:** `d663db8c706cb914e1af5caf651df08edb5c50c0`
- **Exact Source Paths Checked:** `ai-buildweek/lib/privacy-guard.mjs`, `tests/unit/privacy-guard.test.mjs`
- **Source-Behavior Verdict:** `PASS` — Evidenced strictly by observable code and tests (pre-send scanning for private keys, API keys, bearer tokens, connection strings, JWTs, session cookies; two-tier severity model where blockers cause fail-closed rejection and review patterns flag UUIDs/IPs/production markers into `reviewItems`; synthetic fixture domain allowlisting for `@example.com`, `@example.net`, `@example.org`, `@example.test`). Field-level prompt allowlisting truthfully identified as ChangeMesh extension.
- **License/Notice Verdict:** `PASS` (`VERIFIED_COMPATIBLE` — owner-authored).
- **Reuse-Method Verdict:** `PASS` (`CLEAN_ROOM_REIMPLEMENTED` — Python rewrite for Policy Guardian deterministic pre-send gate).
- **Exact Target Mapping Verdict:** `PASS` (`src/policy/policy_guardian.py`).
- **Duplicate/Conflict-Risk Verdict:** `PASS` (Input-side scanning in `policy_guardian.py` complements output-side structural redaction in `domain/contracts/conventions.py`).
- **Forbidden-Carry-Over Verdict:** `PASS` (Excludes `openai_api_key` regex naming, school-SaaS fixture data, and ZeroKit product semantics).
- **Required-Test Verdict:** `PASS` (Blocking patterns fail closed, review patterns populate `reviewItems`, synthetic domain allowlisting, allowlisted prompt fields, forbidden carry-over; `DEFINED != EXECUTED` distinction maintained).
- **WARN Findings:** None.
- **BLOCKED Findings:** None.

#### 4. ZK-VALID-001 — Config Validator / Strict Output Validation
- **Component ID:** `ZK-VALID-001`
- **Donor Repository:** `zyganali-glitch/zerokit-ai-control-plane` (`D-ZEROKIT`)
- **Immutable SHA:** `d663db8c706cb914e1af5caf651df08edb5c50c0`
- **Exact Source Paths Checked:** `frontend/js/config-validator.js`, `tests/unit/config-validator.test.mjs`
- **Source-Behavior Verdict:** `PASS` — Evidenced strictly by observable code and tests (multi-section plain object schema validation requiring registry sections; boolean/finite-number type validation; fail-closed rejection on missing sections or malformed values; non-root-relative endpoints trigger warnings). Intentional delta noted: donor warns on non-root endpoints; ChangeMesh explicitly defines an intentional delta requiring strict path traversal and unsafe endpoint rejection (`OUT-T05`, `OUT-T06`).
- **License/Notice Verdict:** `PASS` (`VERIFIED_COMPATIBLE` — owner-authored).
- **Reuse-Method Verdict:** `PASS` (`CLEAN_ROOM_REIMPLEMENTED` — Python rewrite using Pydantic schemas with `extra="forbid"` for Gemini structured output).
- **Exact Target Mapping Verdict:** `PASS` (`src/core/gemini_structured_output.py`).
- **Duplicate/Conflict-Risk Verdict:** `PASS` (Aligns with ChangeMesh Pydantic domain contracts; informs Gemini structured output validator design).
- **Forbidden-Carry-Over Verdict:** `PASS` (Excludes ZeroKit configuration schema [`panel_registry`, `rbac_registry`, etc.], `validateZeroKitConfig` function name, `school-saas`/`healthcare-saas` fixture scenarios, frontend globals).
- **Required-Test Verdict:** `PASS` (Missing required fields fail closed, extra forbidden fields fail closed, wrong type fails closed, path traversal & unsafe endpoint rejection, conforming outputs, forbidden carry-over; `DEFINED != EXECUTED` distinction maintained).
- **WARN Findings:** None.
- **BLOCKED Findings:** None.

### 10.2 Overall Auditor Verdict

| Metric | Result |
|---|---|
| Components Audited | 4 components across 8 exact allowlisted source/test files |
| Blocking Findings | 0 |
| Warning Findings | 0 |
| Overall Verdict | **PASS** |
